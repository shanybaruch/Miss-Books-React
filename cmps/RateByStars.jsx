export function RateByStars(wayRating) {
    console.log(wayRating.cmpType);

    const [rating, setRating] = useState(0)
    const [hover, setHover] = useState(0)

    return (
        <section className="rate-by-stars">
           
        </section>
    )
}